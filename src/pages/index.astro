---
const name = "Ziqian Zou";

const planets = [
  {
    id: "games",
    title: "Games Planet",
    orbit: "Night Run",
    description: "Games in progress, finished stories, design notes, and the moments worth one more round.",
    x: 24,
    y: 38,
    size: 108,
    tone: "amber",
  },
  {
    id: "travel",
    title: "Travel Planet",
    orbit: "Horizon Log",
    description: "Cities, routes, photos, and small fragments from places that still glow in memory.",
    x: 70,
    y: 30,
    size: 92,
    tone: "cyan",
  },
  {
    id: "projects",
    title: "Projects Planet",
    orbit: "Builder Track",
    description: "Code, papers, tools, experiments, and the versions that survived careful iteration.",
    x: 56,
    y: 50,
    size: 132,
    tone: "violet",
  },
];
---

<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{name}</title>
    <meta name="description" content={`${name} personal website`} />
    <style>
      :root {
        color-scheme: dark;
        --ink: #f4f7fb;
        --muted: rgba(232, 238, 249, 0.68);
        --quiet: rgba(232, 238, 249, 0.44);
        --panel: rgba(8, 14, 30, 0.66);
        --line: rgba(205, 220, 255, 0.18);
        --shadow: rgba(0, 0, 0, 0.42);
      }

      * {
        box-sizing: border-box;
      }

      html {
        min-height: 100%;
        background: #030712;
      }

      body {
        min-height: 240vh;
        margin: 0;
        overflow-x: hidden;
        color: var(--ink);
        font-family:
          Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
          "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
          sans-serif;
        background:
          radial-gradient(circle at 50% 112%, rgba(52, 123, 184, 0.22), transparent 31rem),
          radial-gradient(circle at 28% 14%, rgba(86, 157, 199, 0.18), transparent 22rem),
          radial-gradient(circle at 76% 18%, rgba(213, 109, 141, 0.15), transparent 19rem),
          linear-gradient(180deg, #030814 0%, #071225 48%, #050811 100%);
      }

      body::before,
      body::after {
        position: fixed;
        inset: 0;
        z-index: -3;
        pointer-events: none;
        content: "";
      }

      body::before {
        background-image:
          radial-gradient(circle at 12% 18%, rgba(255, 255, 255, 0.92) 0 1px, transparent 1.5px),
          radial-gradient(circle at 38% 72%, rgba(142, 203, 255, 0.7) 0 1px, transparent 1.6px),
          radial-gradient(circle at 76% 34%, rgba(255, 238, 201, 0.82) 0 1px, transparent 1.4px),
          radial-gradient(circle at 20% 86%, rgba(255, 255, 255, 0.52) 0 0.7px, transparent 1.2px),
          radial-gradient(circle at 92% 64%, rgba(175, 206, 255, 0.5) 0 0.8px, transparent 1.3px),
          radial-gradient(ellipse at 68% 24%, rgba(169, 199, 236, 0.12), transparent 18rem),
          radial-gradient(ellipse at 21% 42%, rgba(255, 206, 145, 0.08), transparent 14rem);
        background-position:
          0 0,
          30px 80px,
          110px 40px,
          48px 120px,
          160px 90px,
          0 0,
          0 0;
        background-size:
          190px 190px,
          260px 260px,
          330px 330px,
          120px 120px,
          460px 460px,
          100% 100%,
          100% 100%;
        opacity: 0.86;
      }

      body::after {
        z-index: -2;
        background:
          linear-gradient(rgba(157, 187, 231, 0.055) 1px, transparent 1px),
          linear-gradient(90deg, rgba(157, 187, 231, 0.045) 1px, transparent 1px);
        background-size: 76px 76px;
        mask-image: radial-gradient(ellipse at 50% 66%, black 0 41%, transparent 75%);
        opacity: 0.8;
        transform: perspective(760px) rotateX(62deg) translateY(18vh);
        transform-origin: center bottom;
      }

      a {
        color: inherit;
      }

      .shell {
        position: relative;
        display: grid;
        min-height: 240vh;
        grid-template-rows: 1fr;
        padding: 0;
      }

      .footer {
        position: fixed;
        right: clamp(16px, 2vw, 28px);
        bottom: clamp(14px, 2vw, 24px);
        left: clamp(16px, 2vw, 28px);
        z-index: 8;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 18px;
        color: var(--quiet);
        font-size: 13px;
      }

      .sky {
        position: fixed;
        inset: 0;
        z-index: 0;
        display: grid;
        min-height: 100vh;
        overflow: hidden;
        place-items: center;
        padding: 0;
      }

      .dome {
        --camp-lift: 0px;
        --forest-far-lift: 0px;
        --planet-depth: 1;
        --ridge-far-lift: 0px;
        --ridge-middle-lift: 0px;
        --scene-start-offset: 8vh;
        position: relative;
        width: 100%;
        min-height: 100vh;
        overflow: hidden;
        border-bottom: 1px solid rgba(221, 232, 255, 0.18);
        border-radius: 0;
        background:
          radial-gradient(ellipse at 50% 100%, rgba(86, 170, 224, 0.19), transparent 38%),
          radial-gradient(ellipse at 50% 48%, rgba(255, 255, 255, 0.08), transparent 58%);
        box-shadow:
          inset 0 -40px 90px rgba(32, 101, 157, 0.17),
          0 36px 80px rgba(0, 0, 0, 0.24);
      }

      .dome::after {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 24%;
        left: 0;
        z-index: 1;
        background:
          radial-gradient(circle at 16% 22%, rgba(255, 255, 255, 0.82) 0 1px, transparent 1.5px),
          radial-gradient(circle at 64% 18%, rgba(185, 215, 255, 0.74) 0 1px, transparent 1.6px),
          radial-gradient(circle at 83% 42%, rgba(255, 230, 184, 0.66) 0 0.9px, transparent 1.5px),
          radial-gradient(circle at 42% 54%, rgba(255, 255, 255, 0.42) 0 0.7px, transparent 1.3px),
          radial-gradient(ellipse at 71% 27%, rgba(104, 154, 218, 0.16), transparent 12rem),
          radial-gradient(ellipse at 27% 34%, rgba(217, 136, 162, 0.1), transparent 10rem);
        background-position:
          0 0,
          70px 40px,
          20px 100px,
          130px 20px,
          0 0,
          0 0;
        background-size:
          150px 150px,
          230px 230px,
          310px 310px,
          90px 90px,
          100% 100%,
          100% 100%;
        opacity: 0.95;
        pointer-events: none;
        content: "";
      }

      .intro {
        position: absolute;
        top: clamp(34px, 8vh, 76px);
        left: clamp(22px, 6vw, 78px);
        z-index: 6;
        max-width: min(410px, calc(100% - 44px));
      }

      h1 {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        align-items: baseline;
        max-width: 100%;
        margin: 0;
        color: #a9c7ec;
        font-size: 16px;
        font-weight: 500;
        line-height: 1.4;
        letter-spacing: 0;
      }

      .identity-name {
        color: var(--ink);
        font-weight: 800;
      }

      .orbit-field {
        position: absolute;
        top: 50%;
        left: 50%;
        z-index: 2;
        width: 136%;
        height: 136%;
        transform: translate(-50%, -50%) scale(var(--planet-depth));
        transform-origin: 50% 50%;
        transition:
          opacity 900ms ease,
          transform 80ms linear;
      }

      .planet {
        --planet-size: 96px;
        --planet-a: #f4b456;
        --planet-b: #8b4720;
        --planet-c: #fff2c8;
        position: absolute;
        left: calc(var(--x) * 1%);
        top: calc(var(--y) * 1%);
        width: var(--planet-size);
        height: var(--planet-size);
        padding: 0;
        border: 0;
        border-radius: 50%;
        color: inherit;
        cursor: pointer;
        transform: translate(-50%, -50%);
        background:
          radial-gradient(circle at 32% 24%, var(--planet-c) 0 7%, transparent 8%),
          radial-gradient(circle at 35% 31%, var(--planet-a) 0 18%, transparent 36%),
          radial-gradient(circle at 62% 68%, rgba(0, 0, 0, 0.35), transparent 42%),
          radial-gradient(circle at 50% 50%, var(--planet-a), var(--planet-b) 72%);
        box-shadow:
          inset -18px -20px 24px rgba(0, 0, 0, 0.34),
          inset 10px 8px 18px rgba(255, 255, 255, 0.18),
          0 0 0 1px rgba(255, 255, 255, 0.13),
          0 0 46px color-mix(in srgb, var(--planet-a), transparent 34%);
        transition:
          transform 180ms ease,
          filter 180ms ease,
          box-shadow 180ms ease;
      }

      .planet::after {
        position: absolute;
        inset: -8px;
        border-radius: inherit;
        background: radial-gradient(circle, color-mix(in srgb, var(--planet-a), transparent 52%), transparent 66%);
        filter: blur(14px);
        opacity: 0.72;
        z-index: -1;
        content: "";
      }

      .planet:hover,
      .planet:focus-visible,
      .planet.is-active {
        filter: saturate(1.22) brightness(1.1);
        outline: none;
        transform: translate(-50%, -50%) scale(1.08);
      }

      .planet[data-tone="cyan"] {
        --planet-a: #77d9ee;
        --planet-b: #1c5b78;
        --planet-c: #e9ffff;
      }

      .planet[data-tone="violet"] {
        --planet-a: #a996ff;
        --planet-b: #493a8c;
        --planet-c: #fff4ff;
      }

      .planet-label {
        position: absolute;
        top: calc(100% + 13px);
        left: 50%;
        width: max-content;
        max-width: 150px;
        color: rgba(244, 247, 251, 0.88);
        font-size: 14px;
        font-weight: 650;
        opacity: 0;
        pointer-events: none;
        text-align: center;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.8);
        transform: translateX(-50%);
        transition: opacity 160ms ease, transform 160ms ease;
      }

      .planet:hover .planet-label,
      .planet:focus-visible .planet-label,
      .planet.is-active .planet-label {
        opacity: 1;
        transform: translateX(-50%) translateY(2px);
      }

      .info-card {
        position: absolute;
        left: 0;
        top: 0;
        z-index: 7;
        width: min(292px, calc(100% - 36px));
        padding: 16px;
        border: 1px solid var(--line);
        border-radius: 8px;
        background:
          linear-gradient(135deg, rgba(255, 255, 255, 0.08), transparent 42%),
          var(--panel);
        box-shadow: 0 18px 44px var(--shadow);
        backdrop-filter: blur(18px);
        opacity: 0;
        pointer-events: none;
        transform: translate3d(0, 10px, 0) scale(0.98);
        transition:
          opacity 150ms ease,
          transform 150ms ease;
      }

      .info-card.is-visible {
        opacity: 1;
        transform: translate3d(0, 0, 0) scale(1);
      }

      .info-card p {
        margin: 0;
      }

      .info-kicker {
        color: #a9c7ec;
        font-size: 12px;
        font-weight: 600;
        line-height: 1.4;
      }

      .info-title {
        margin: 8px 0;
        font-size: 16px;
        font-weight: 800;
        line-height: 1.35;
      }

      .info-desc {
        color: var(--muted);
        font-size: 13px;
        line-height: 1.65;
      }

      .horizon {
        position: absolute;
        right: 0;
        bottom: -78vh;
        left: 0;
        z-index: 4;
        height: 76vh;
        border-radius: 0;
        background:
          linear-gradient(180deg, #18243a 0%, #111b2c 44%, #050913 100%);
        pointer-events: none;
        transform: translate3d(0, calc(var(--scene-start-offset) - var(--camp-lift)), 0);
        transition: transform 80ms linear;
      }

      .landscape-layers {
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
        z-index: 3;
        height: 100vh;
        overflow: hidden;
        pointer-events: none;
        transform: translate3d(0, var(--scene-start-offset), 0);
        transition: transform 80ms linear;
      }

      .landscape-layer {
        position: absolute;
        right: 0;
        left: 0;
        line-height: 0;
      }

      .landscape-layer svg,
      .ground-forest svg {
        display: block;
        width: 100%;
        height: 100%;
        overflow: visible;
      }

      .ridge-far {
        bottom: -20vh;
        height: 90vh;
        opacity: 1;
        transform: translate3d(0, calc(var(--ridge-far-lift) * -1), 0);
        transition: transform 80ms linear;
      }

      .camp-item.ridge-lighthouse {
        position: static;
        display: inline;
        height: auto;
        padding: 0;
        border: 0;
        background: transparent;
        line-height: normal;
        opacity: 0.78;
        cursor: pointer;
        pointer-events: auto;
        transform: none;
        transform-box: fill-box;
        transform-origin: center;
        appearance: auto;
        user-select: none;
      }

      .ridge-lighthouse-hit {
        fill: none;
        pointer-events: all;
        stroke: none;
      }

      .ridge-lighthouse-image {
        pointer-events: none;
        user-select: none;
      }

      .ridge-middle {
        bottom: -35vh;
        height: 71vh;
        opacity: 0.9;
        transform: translate3d(0, calc(var(--ridge-middle-lift) * -1), 0);
        transform-origin: 27% 47%;
        transition: transform 80ms linear;
      }

      .ridge-middle-frame {
        position: relative;
        width: 100%;
        height: 100%;
      }

      .forest-far {
        bottom: -30vh;
        height: 31vh;
        opacity: 0.92;
        transform: translate3d(0, calc(var(--forest-far-lift) * -1), 0);
        transition: transform 80ms linear;
      }

      .ground-forest {
        position: absolute;
        left: 50%;
        bottom: -5vh;
        z-index: 5;
        width: 100vw;
        height: clamp(132px, 16vw, 220px);
        pointer-events: none;
        transform: translate3d(-50%, calc(var(--scene-start-offset) - var(--camp-lift)), 0);
        transition: transform 80ms linear;
      }

      .fire-reflection {
        position: absolute;
        left: calc(50% + clamp(48px, 6.5vw, 82px));
        bottom: calc(-58vh + var(--camp-lift));
        z-index: 5;
        width: clamp(250px, 33vw, 420px);
        height: clamp(96px, 12vw, 156px);
        border-radius: 50%;
        background:
          radial-gradient(ellipse at 50% 42%, rgba(255, 151, 55, 0.34), transparent 30%),
          radial-gradient(ellipse at 50% 58%, rgba(255, 209, 111, 0.2), transparent 62%);
        filter: blur(12px);
        opacity: 0.72;
        pointer-events: none;
        transform: translate3d(-50%, var(--scene-start-offset), 0);
      }

      .camp-scene {
        position: absolute;
        left: 50%;
        bottom: -44vh;
        z-index: 6;
        width: clamp(360px, 48vw, 610px);
        height: clamp(170px, 22vw, 276px);
        pointer-events: auto;
        transform: translate3d(-50%, calc(var(--scene-start-offset) - var(--camp-lift)), 0);
        transition: transform 80ms linear;
      }

      body.is-lighthouse-focus .camp-dialog,
      .dome.is-lighthouse-focus .intro,
      .dome.is-lighthouse-focus .footer,
      .dome.is-lighthouse-focus .info-card {
        opacity: 0;
        pointer-events: none;
      }

      .dome.is-lighthouse-focus .planet,
      .dome.is-lighthouse-focus .camp-item:not(.ridge-lighthouse) {
        pointer-events: none;
      }

      .dome.is-lighthouse-focus .orbit-field {
        opacity: 0.26;
        transform: translate(-50%, -54%) scale(0.76);
        transition:
          opacity 1200ms ease,
          transform 1800ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .landscape-layers {
        transform: translate3d(0, 0, 0);
        transition: transform 1800ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .ridge-far {
        opacity: 0.56;
        transform: translate3d(7vw, -8vh, 0) scale(1.12);
        transition:
          opacity 1500ms ease,
          transform 1800ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .ridge-middle {
        opacity: 1;
        transform: translate3d(23vw, -11vh, 0) scale(2.75);
        transition:
          opacity 1200ms ease,
          transform 1900ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .forest-far {
        transform: translate3d(0, 76vh, 0) scale(1.08);
        transition: transform 1700ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .horizon {
        transform: translate3d(0, 94vh, 0);
        transition: transform 1500ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .ground-forest {
        transform: translate3d(-50%, 112vh, 0);
        transition: transform 1300ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .camp-scene {
        transform: translate3d(-50%, 126vh, 0);
        transition: transform 1200ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .fire-reflection {
        opacity: 0;
        transform: translate3d(-50%, 120vh, 0);
        transition:
          opacity 700ms ease,
          transform 1200ms cubic-bezier(0.2, 0.72, 0.12, 1);
      }

      .dome.is-lighthouse-focus .ridge-lighthouse {
        opacity: 1;
        filter:
          drop-shadow(0 0 18px rgba(255, 244, 192, 0.34))
          drop-shadow(0 0 42px rgba(255, 244, 192, 0.16));
      }

      .camp-item {
        position: absolute;
        display: block;
        height: auto;
        padding: 0;
        border: 0;
        background: transparent;
        color: inherit;
        line-height: 0;
        cursor: pointer;
        pointer-events: auto;
        touch-action: manipulation;
        user-select: none;
        transform-origin: 50% 78%;
        transition:
          transform 180ms ease,
          opacity 180ms ease,
          filter 180ms ease;
        appearance: none;
      }

      .camp-visual {
        position: relative;
        z-index: 2;
        display: block;
        width: 100%;
        height: auto;
        object-fit: contain;
        pointer-events: none;
        user-select: none;
      }

      .camp-item:focus-visible {
        outline: none;
      }

      .camp-car {
        left: 0;
        bottom: 16%;
        z-index: 1;
        width: 39%;
        --fire-lit-filter: brightness(0.84) saturate(0.92);
        filter: var(--fire-lit-filter);
      }

      .cast-shadow {
        position: absolute;
        z-index: 1;
        border-radius: 50%;
        background: radial-gradient(ellipse at 50% 50%, rgba(0, 0, 0, 0.68), transparent 70%);
        filter: blur(8px);
        opacity: 0.72;
        pointer-events: none;
        transform-origin: 50% 50%;
      }

      .car-shadow {
        left: -16%;
        bottom: -6%;
        width: 128%;
        height: 80%;
        transform: rotate(8deg) skewX(-26deg);
        opacity: 0.64;
      }

      .people-shadow {
        left: -31%;
        bottom: 18%;
        width: 108%;
        height: 28%;
        transform: rotate(10deg) skewX(-28deg);
      }

      .dog-shadow {
        left: 62%;
        bottom: 18%;
        width: 106%;
        height: 25%;
        transform: rotate(-12deg) skewX(-28deg);
      }

      .people-reflection {
        position: absolute;
        left: 43%;
        top: 31%;
        z-index: 3;
        width: 20%;
        height: 34%;
        border-radius: 48% 52% 56% 44%;
        background:
          radial-gradient(ellipse at 45% 24%, rgba(17, 19, 25, 0.32), transparent 24%),
          linear-gradient(165deg, transparent 0 24%, rgba(15, 18, 26, 0.32) 25% 62%, transparent 63%);
        filter: blur(3px);
        mix-blend-mode: multiply;
        opacity: 0.28;
        pointer-events: none;
        transform: rotate(-8deg) skewX(-9deg);
      }

      .camp-people {
        left: 32%;
        bottom: 8%;
        z-index: 3;
        width: 28%;
        --fire-lit-filter: brightness(0.8) saturate(0.9);
        filter: var(--fire-lit-filter);
      }

      .camp-fire {
        left: 53%;
        bottom: 0;
        z-index: 4;
        width: 21%;
        aspect-ratio: 1 / 1;
        filter: drop-shadow(0 0 18px rgba(255, 134, 48, 0.24));
      }

      .camp-fire-part {
        position: absolute;
        left: 0;
        width: 100%;
        height: auto;
        object-fit: contain;
        pointer-events: none;
        user-select: none;
      }

      .camp-wood {
        bottom: 0;
        z-index: 1;
      }

      .camp-fire-gif {
        position: absolute;
        left: 50%;
        bottom: 30%;
        z-index: 2;
        width: 88%;
        height: auto;
        object-fit: contain;
        pointer-events: none;
        transform: translateX(-50%);
        mix-blend-mode: screen;
        filter: drop-shadow(0 0 16px rgba(255, 143, 46, 0.3));
        user-select: none;
      }

      .dome.is-camp-extinguished .camp-fire {
        filter: none;
      }

      .dome.is-camp-extinguished .camp-fire-gif,
      .dome.is-camp-extinguished .fire-sparks,
      .dome.is-camp-extinguished .fire-reflection,
      .dome.is-camp-extinguished .people-reflection {
        opacity: 0;
      }

      .dome.is-camp-extinguished .fire-reflection {
        animation: none;
      }

      .dome.is-camp-extinguished .camp-car,
      .dome.is-camp-extinguished .camp-people,
      .dome.is-camp-extinguished .camp-dog {
        animation: none;
        filter: brightness(0.38) saturate(0.38) contrast(0.92);
      }

      .dome.is-camp-extinguished .camp-dog .camp-visual {
        animation: none;
      }

      .dome.is-camp-extinguished .camp-wood {
        opacity: 0.82;
        filter: brightness(0.88) saturate(0.78);
      }

      .dome.is-camp-extinguished .cast-shadow {
        animation: none;
        opacity: 0.48;
      }

      .campfire-modal {
        position: fixed;
        inset: 0;
        z-index: 20;
        display: grid;
        place-items: center;
        padding: 24px;
        background: rgba(2, 6, 16, 0.72);
        backdrop-filter: blur(10px);
      }

      .campfire-modal[hidden] {
        display: none;
      }

      .campfire-panel {
        position: relative;
        width: min(420px, calc(100vw - 32px));
        padding: 24px;
        border: 1px solid rgba(255, 202, 121, 0.2);
        border-radius: 8px;
        background:
          radial-gradient(ellipse at 32% 32%, rgba(255, 139, 45, 0.18), transparent 36%),
          linear-gradient(180deg, rgba(16, 23, 39, 0.96), rgba(5, 9, 18, 0.98));
        box-shadow:
          0 28px 80px rgba(0, 0, 0, 0.48),
          inset 0 1px 0 rgba(255, 255, 255, 0.06);
      }

      .campfire-close {
        position: absolute;
        top: 12px;
        right: 12px;
        z-index: 3;
        display: grid;
        width: 34px;
        height: 34px;
        place-items: center;
        border: 1px solid rgba(231, 238, 251, 0.16);
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.04);
        color: rgba(244, 247, 251, 0.82);
        cursor: pointer;
      }

      .campfire-close:hover,
      .campfire-close:focus-visible {
        border-color: rgba(255, 202, 121, 0.36);
        outline: none;
      }

      .campfire-stage {
        position: relative;
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(96px, 0.72fr);
        align-items: end;
        gap: 18px;
        min-height: 176px;
        margin-bottom: 8px;
      }

      .campfire-visual {
        position: relative;
        display: grid;
        min-height: 164px;
        place-items: center;
      }

      .campfire-visual::before {
        position: absolute;
        width: 150px;
        height: 180px;
        border-radius: 50%;
        background: radial-gradient(ellipse at 50% 50%, rgba(255, 139, 45, 0.2), transparent 68%);
        filter: blur(14px);
        content: "";
        transform: translate(0, 10px);
      }

      .campfire-visual img {
        position: relative;
        z-index: 1;
        width: 132px;
        height: auto;
        mix-blend-mode: screen;
      }

      .campfire-modal.is-extinguished .campfire-visual::before {
        background: radial-gradient(ellipse at 50% 50%, rgba(132, 146, 166, 0.08), transparent 68%);
      }

      .campfire-modal.is-extinguished .campfire-visual img {
        mix-blend-mode: normal;
        opacity: 0.86;
        filter: brightness(0.88) saturate(0.78);
      }

      .campfire-wood-stack {
        position: relative;
        display: grid;
        place-items: end center;
        min-height: 126px;
        border: 0;
        background: transparent;
        cursor: grab;
        touch-action: none;
      }

      .campfire-wood-stack:focus-visible {
        outline: 1px solid rgba(255, 202, 121, 0.58);
        outline-offset: 4px;
      }

      .campfire-wood-stack:disabled,
      .campfire-wood-stack[aria-disabled="true"] {
        cursor: not-allowed;
        opacity: 0.46;
      }

      .campfire-wood-stack img {
        width: min(112px, 27vw);
        height: auto;
        filter: drop-shadow(0 14px 18px rgba(0, 0, 0, 0.32));
        user-select: none;
        -webkit-user-drag: none;
      }

      .campfire-drag-log {
        position: fixed;
        top: 0;
        left: 0;
        z-index: 30;
        width: 118px;
        height: auto;
        opacity: 0;
        pointer-events: none;
        transform: translate3d(-999px, -999px, 0) rotate(-8deg);
        filter: drop-shadow(0 16px 18px rgba(0, 0, 0, 0.42));
        transition: opacity 120ms ease;
        user-select: none;
      }

      .campfire-drag-log.is-visible {
        opacity: 0.94;
      }

      .campfire-title {
        margin: 0;
        font-size: 20px;
        font-weight: 800;
        line-height: 1.25;
        text-align: center;
      }

      .campfire-copy {
        min-height: 24px;
        margin: 10px 0 18px;
        color: rgba(232, 238, 249, 0.68);
        font-size: 13px;
        line-height: 1.7;
        text-align: center;
      }

      .campfire-meter {
        display: grid;
        gap: 8px;
        margin-bottom: 18px;
      }

      .campfire-meter-label {
        display: flex;
        justify-content: space-between;
        color: rgba(244, 247, 251, 0.78);
        font-size: 12px;
        font-weight: 700;
      }

      .campfire-track {
        height: 8px;
        overflow: hidden;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
      }

      .campfire-fill {
        width: calc(var(--campfire-level, 0) * 1%);
        height: 100%;
        border-radius: inherit;
        background: linear-gradient(90deg, #f97316, #facc15);
        box-shadow: 0 0 18px rgba(255, 161, 54, 0.42);
        transition: width 220ms ease;
      }

      .campfire-secondary-action {
        width: 100%;
        min-height: 38px;
        margin-top: 10px;
        border: 1px solid rgba(232, 238, 249, 0.16);
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.04);
        color: rgba(232, 238, 249, 0.72);
        cursor: pointer;
        font: inherit;
        font-size: 13px;
        font-weight: 700;
      }

      .campfire-secondary-action:hover:not(:disabled),
      .campfire-secondary-action:focus-visible:not(:disabled) {
        border-color: rgba(255, 202, 121, 0.28);
        color: rgba(244, 247, 251, 0.92);
        outline: none;
      }

      .campfire-secondary-action:disabled {
        cursor: not-allowed;
        opacity: 0.42;
      }

      .campfire-status {
        min-height: 18px;
        margin: 12px 0 0;
        color: rgba(232, 238, 249, 0.52);
        font-size: 12px;
        line-height: 1.5;
        text-align: center;
      }

      .camp-dialog {
        position: fixed;
        right: 24px;
        bottom: 24px;
        left: 24px;
        z-index: 18;
        display: flex;
        justify-content: center;
        pointer-events: none;
      }

      .camp-dialog[hidden] {
        display: none;
      }

      .camp-dialog-panel {
        position: relative;
        width: min(560px, 100%);
        padding: 18px 48px 18px 20px;
        border: 1px solid rgba(185, 205, 231, 0.22);
        border-radius: 8px;
        background:
          linear-gradient(180deg, rgba(18, 29, 47, 0.96), rgba(7, 12, 23, 0.98));
        box-shadow:
          0 20px 70px rgba(0, 0, 0, 0.42),
          inset 0 1px 0 rgba(255, 255, 255, 0.06);
        pointer-events: auto;
      }

      .camp-dialog-speaker {
        margin: 0 0 7px;
        color: rgba(195, 216, 245, 0.78);
        font-size: 12px;
        font-weight: 800;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }

      .camp-dialog-line {
        min-height: 24px;
        margin: 0;
        color: rgba(244, 247, 251, 0.9);
        font-size: 15px;
        font-weight: 650;
        line-height: 1.65;
      }

      .camp-dialog-close {
        position: absolute;
        top: 12px;
        right: 12px;
        display: grid;
        width: 30px;
        height: 30px;
        place-items: center;
        border: 1px solid rgba(232, 238, 249, 0.14);
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.04);
        color: rgba(244, 247, 251, 0.76);
        cursor: pointer;
      }

      .camp-dialog-close:hover,
      .camp-dialog-close:focus-visible {
        border-color: rgba(195, 216, 245, 0.32);
        outline: none;
      }

      .lighthouse-entry {
        position: fixed;
        right: 24px;
        bottom: 28px;
        left: 24px;
        z-index: 19;
        display: flex;
        justify-content: center;
        opacity: 0;
        pointer-events: none;
        transform: translate3d(0, 12px, 0);
        transition:
          opacity 420ms ease,
          transform 420ms ease;
      }

      .lighthouse-entry[hidden] {
        display: none;
      }

      .lighthouse-entry.is-visible {
        opacity: 1;
        pointer-events: auto;
        transform: translate3d(0, 0, 0);
      }

      .lighthouse-entry-panel {
        width: min(390px, 100%);
        padding: 18px;
        border: 1px solid rgba(255, 244, 192, 0.2);
        border-radius: 8px;
        background:
          radial-gradient(ellipse at 50% 0%, rgba(255, 244, 192, 0.14), transparent 48%),
          linear-gradient(180deg, rgba(17, 27, 44, 0.96), rgba(5, 9, 18, 0.98));
        box-shadow:
          0 22px 70px rgba(0, 0, 0, 0.44),
          inset 0 1px 0 rgba(255, 255, 255, 0.06);
        text-align: center;
      }

      .lighthouse-entry-title {
        margin: 0 0 6px;
        color: rgba(255, 250, 224, 0.94);
        font-size: 18px;
        font-weight: 850;
      }

      .lighthouse-entry-copy {
        margin: 0 0 16px;
        color: rgba(232, 238, 249, 0.62);
        font-size: 13px;
        line-height: 1.55;
      }

      .lighthouse-entry-actions {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 10px;
      }

      .lighthouse-entry-link,
      .lighthouse-entry-cancel {
        display: inline-flex;
        min-height: 40px;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        font: inherit;
        font-size: 13px;
        font-weight: 800;
        text-decoration: none;
      }

      .lighthouse-entry-link {
        border: 0;
        background: linear-gradient(180deg, #fff0b9, #f7bf59);
        color: #1d1507;
        box-shadow: 0 10px 26px rgba(247, 191, 89, 0.2);
      }

      .lighthouse-entry-cancel {
        border: 1px solid rgba(232, 238, 249, 0.16);
        background: rgba(255, 255, 255, 0.04);
        color: rgba(232, 238, 249, 0.72);
        cursor: pointer;
      }

      .lighthouse-entry-link:hover,
      .lighthouse-entry-link:focus-visible,
      .lighthouse-entry-cancel:hover,
      .lighthouse-entry-cancel:focus-visible {
        filter: brightness(1.06);
        outline: none;
      }

      .fire-sparks {
        position: absolute;
        left: 63.5%;
        bottom: 31%;
        z-index: 5;
        width: 58px;
        height: 68px;
        pointer-events: none;
        transform: translateX(-50%);
      }

      .fire-sparks span {
        position: absolute;
        bottom: 0;
        left: 50%;
        width: var(--spark-size, 4px);
        height: var(--spark-size, 4px);
        border-radius: 50%;
        background: rgba(255, 196, 93, 0.92);
        box-shadow: 0 0 10px rgba(255, 139, 45, 0.72);
        opacity: 0;
      }

      .fire-sparks .spark-small {
        --spark-size: 2.5px;
        background: rgba(255, 210, 130, 0.82);
        box-shadow: 0 0 7px rgba(255, 139, 45, 0.5);
      }

      .fire-sparks span:nth-child(1) {
        margin-left: -18px;
      }

      .fire-sparks span:nth-child(2) {
        margin-left: -8px;
      }

      .fire-sparks span:nth-child(3) {
        margin-left: 6px;
      }

      .fire-sparks span:nth-child(4) {
        margin-left: 18px;
      }

      .fire-sparks span:nth-child(5) {
        margin-left: 0;
      }

      .fire-sparks span:nth-child(6) {
        margin-left: 13px;
      }

      .camp-dog {
        right: 8%;
        bottom: 9%;
        z-index: 2;
        width: 16%;
        cursor: url("/camp/cursor_dog_hover.png") 18 20, pointer;
        --fire-lit-filter: brightness(0.9) saturate(0.86);
        filter: var(--fire-lit-filter);
      }

      .camp-dog .camp-visual {
        transform-origin: 50% 92%;
        animation: dog-breathe 5.2s ease-in-out infinite;
      }

      .footer {
        padding: 0;
      }

      @media (max-width: 760px) {
        .shell {
          padding: 0;
        }

        .footer {
          align-items: flex-start;
          flex-direction: column;
        }

        .sky {
          min-height: 100vh;
          padding-top: 0;
        }

        .dome {
          min-height: 100vh;
          border-radius: 0;
        }

        .intro {
          top: 28px;
          left: 20px;
        }

        .planet {
          --planet-size: calc(var(--size) * 0.72px);
        }

        #games {
          left: 27%;
          top: 42%;
        }

        #travel {
          left: 73%;
          top: 37%;
        }

        #projects {
          --planet-size: calc(var(--size) * 0.55px);
          left: 56%;
          top: 56%;
        }

        .planet-label {
          max-width: 112px;
          white-space: normal;
        }

        .info-card {
          width: min(270px, calc(100% - 28px));
        }
      }

      @media (min-width: 761px) {
        .planet {
          --planet-size: calc(var(--size) * 1px);
        }
      }

      @media (prefers-reduced-motion: no-preference) {
        body::before {
          animation: stars-drift 38s linear infinite;
        }

        .planet {
          animation: planet-float 7s ease-in-out infinite;
          animation-delay: calc(var(--x) * -80ms);
        }

        .fire-sparks span {
          animation: spark-rise-bright 3.6s ease-out infinite;
        }

        .fire-sparks span:nth-child(2) {
          animation-delay: 0.18s;
        }

        .fire-sparks span:nth-child(3) {
          animation-delay: 0.36s;
        }

        .fire-sparks span:nth-child(4) {
          animation-delay: 0.54s;
        }

        .fire-sparks .spark-small {
          animation-name: spark-rise-dim;
        }

        .fire-sparks .spark-small:nth-child(5) {
          animation-delay: 0.12s;
        }

        .fire-sparks .spark-small:nth-child(6) {
          animation-delay: 0.46s;
        }

        .camp-car,
        .camp-people,
        .camp-dog {
          animation: firelight-shimmer 3.6s ease-in-out infinite;
        }

        .cast-shadow {
          animation: cast-shadow-shimmer 3.6s ease-in-out infinite;
        }

        .people-reflection {
          animation: reflection-shimmer 3.6s ease-in-out infinite;
        }

        .fire-reflection {
          animation: ground-firelight 3.6s ease-in-out infinite;
        }
      }

      @keyframes stars-drift {
        to {
          background-position:
            190px 190px,
            290px 340px,
            440px 370px;
        }
      }

      @keyframes planet-float {
        0%,
        100% {
          margin-top: -5px;
        }

        50% {
          margin-top: 7px;
        }
      }

      @keyframes spark-rise-bright {
        0% {
          opacity: 0;
          transform: translate3d(0, 0, 0) scale(0.55);
        }

        28% {
          opacity: 0.92;
          transform: translate3d(0, -12px, 0) scale(0.9);
        }

        62% {
          opacity: 0.48;
          transform: translate3d(calc(var(--spark-drift, 8px) * 0.58), -42px, 0) scale(0.42);
        }

        100% {
          opacity: 0;
          transform: translate3d(var(--spark-drift, 8px), -66px, 0) scale(0.16);
        }
      }

      @keyframes spark-rise-dim {
        0% {
          opacity: 0.62;
          transform: translate3d(0, 0, 0) scale(0.82);
        }

        22% {
          opacity: 0.38;
          transform: translate3d(calc(var(--spark-drift, 8px) * 0.35), -20px, 0) scale(0.52);
        }

        36%,
        72% {
          opacity: 0;
        }

        82% {
          opacity: 0.5;
          transform: translate3d(0, 0, 0) scale(0.72);
        }

        100% {
          opacity: 0;
          transform: translate3d(var(--spark-drift, 8px), -34px, 0) scale(0.28);
        }
      }

      @keyframes firelight-shimmer {
        0%,
        100% {
          filter: var(--fire-lit-filter) drop-shadow(0 0 0 rgba(255, 134, 48, 0));
        }

        22% {
          filter: brightness(0.88) saturate(0.92) drop-shadow(0 0 2px rgba(255, 139, 45, 0.06));
        }

        42% {
          filter: brightness(1.12) saturate(1.12) drop-shadow(0 0 13px rgba(255, 150, 54, 0.22));
        }

        58% {
          filter: brightness(1.04) saturate(1.06) drop-shadow(0 0 8px rgba(255, 139, 45, 0.16));
        }

        76% {
          filter: brightness(0.9) saturate(0.94) drop-shadow(0 0 3px rgba(255, 139, 45, 0.08));
        }
      }

      @keyframes dog-breathe {
        0%,
        100% {
          transform: scaleY(1);
        }

        50% {
          transform: scaleY(1.03);
        }
      }

      @keyframes cast-shadow-shimmer {
        0%,
        100% {
          opacity: 0.62;
          filter: blur(7px);
        }

        42% {
          opacity: 0.76;
          filter: blur(8px);
        }

        76% {
          opacity: 0.5;
          filter: blur(6px);
        }
      }

      @keyframes reflection-shimmer {
        0%,
        100% {
          opacity: 0.22;
          filter: blur(3.5px);
        }

        42% {
          opacity: 0.38;
          filter: blur(4px);
        }

        76% {
          opacity: 0.18;
          filter: blur(3px);
        }
      }

      @keyframes ground-firelight {
        0%,
        100% {
          opacity: 0.42;
          transform: translateX(-50%) scale(0.9, 0.84);
        }

        22% {
          opacity: 0.5;
          transform: translateX(-50%) scale(0.96, 0.9);
        }

        42% {
          opacity: 0.96;
          transform: translateX(-50%) scale(1.12, 1.08);
        }

        58% {
          opacity: 0.76;
          transform: translateX(-50%) scale(1.04, 1);
        }

        76% {
          opacity: 0.48;
          transform: translateX(-50%) scale(0.94, 0.88);
        }
      }
    </style>
  </head>

  <body>
    <main class="shell">
      <section class="sky" aria-label="个人星图">
        <div class="dome">
          <div class="intro">
            <h1><span>Operation Astro</span><span class="identity-name">{name}</span></h1>
          </div>

          <div class="orbit-field" aria-label="Planet entries">
            {
              planets.map((planet) => (
                <button
                  class="planet"
                  id={planet.id}
                  type="button"
                  data-title={planet.title}
                  data-orbit={planet.orbit}
                  data-description={planet.description}
                  data-tone={planet.tone}
                  style={`--x: ${planet.x}; --y: ${planet.y}; --size: ${planet.size};`}
                  aria-label={`${planet.title}: ${planet.description}`}
                >
                  <span class="planet-label">{planet.title}</span>
                </button>
              ))
            }
          </div>

          <aside class="info-card" aria-live="polite" aria-hidden="true">
            <p class="info-kicker" data-info-orbit></p>
            <h2 class="info-title" data-info-title></h2>
            <p class="info-desc" data-info-description></p>
          </aside>

          <div class="landscape-layers">
            <div class="landscape-layer ridge-far">
              <svg viewBox="0 0 1200 260" preserveAspectRatio="none" focusable="false">
                <path
                  d="M0 132 L92 116 L184 126 L278 96 L372 116 L464 92 L558 120 L660 86 L764 118 L864 100 L984 126 L1102 106 L1200 122 L1200 260 L0 260 Z"
                  fill="#3a475c"
                />
                <path
                  d="M0 156 L128 136 L240 154 L364 122 L494 148 L618 116 L750 150 L872 128 L1008 158 L1200 132 L1200 260 L0 260 Z"
                  fill="#2d3a50"
                  opacity="0.62"
                />
              </svg>
            </div>

            <div class="landscape-layer ridge-middle">
              <div class="ridge-middle-frame">
                <svg viewBox="0 0 1200 300" preserveAspectRatio="none" focusable="false">
                  <path
                    d="M0 154 L78 130 L162 168 L262 96 L344 142 L438 86 L548 158 L640 112 L736 176 L840 104 L938 148 L1036 116 L1200 170 L1200 300 L0 300 Z"
                    fill="#253247"
                  />
                  <path
                    d="M0 194 L108 160 L228 206 L342 132 L474 190 L610 144 L744 206 L876 150 L1016 196 L1200 162 L1200 300 L0 300 Z"
                    fill="#18243a"
                    opacity="1"
                  />
                  <g
                    class="camp-item ridge-lighthouse"
                    role="button"
                    tabindex="0"
                    data-camp-item="lighthouse"
                    data-camp-label="Lighthouse"
                    aria-label="Interact with the lighthouse"
                  >
                    <rect class="ridge-lighthouse-hit" x="248" y="88" width="104" height="104" />
                    <image
                      class="ridge-lighthouse-image"
                      href="/camp/lighthouse.png"
                      x="260"
                      y="89"
                      width="120"
                      height="80"
                      preserveAspectRatio="none"
                    />
                  </g>
                </svg>
              </div>
            </div>

            <div class="landscape-layer forest-far">
              <svg viewBox="0 0 1200 320" preserveAspectRatio="none" focusable="false">
                <g fill="#0b1726">
                  <polygon points="-2,256 18,190 38,256" opacity="0.72" />
                  <polygon points="24,252 45,170 66,252" opacity="0.84" />
                  <polygon points="52,258 72,188 92,258" opacity="0.68" />
                  <polygon points="78,250 100,162 122,250" />
                  <polygon points="106,256 128,180 150,256" opacity="0.78" />
                  <polygon points="132,260 156,166 180,260" opacity="0.9" />
                  <polygon points="162,254 186,184 210,254" opacity="0.72" />
                  <polygon points="194,250 216,172 238,250" opacity="0.82" />
                  <polygon points="224,258 246,196 268,258" opacity="0.58" />
                  <polygon points="254,252 278,176 302,252" opacity="0.76" />
                  <polygon points="286,262 310,194 334,262" opacity="0.56" />
                  <polygon points="320,256 344,188 368,256" opacity="0.58" />
                  <polygon points="362,258 382,206 402,258" opacity="0.46" />
                  <polygon points="400,252 420,198 440,252" opacity="0.48" />
                  <polygon points="438,260 458,210 478,260" opacity="0.42" />
                  <polygon points="476,256 496,214 516,256" opacity="0.38" />
                  <polygon points="514,262 534,218 554,262" opacity="0.36" />
                  <polygon points="552,258 572,216 592,258" opacity="0.36" />
                  <polygon points="590,262 610,220 630,262" opacity="0.34" />
                  <polygon points="628,256 648,214 668,256" opacity="0.38" />
                  <polygon points="666,260 686,210 706,260" opacity="0.42" />
                  <polygon points="704,254 724,202 744,254" opacity="0.48" />
                  <polygon points="742,258 762,194 782,258" opacity="0.56" />
                  <polygon points="778,252 800,180 822,252" opacity="0.7" />
                  <polygon points="812,260 834,190 856,260" opacity="0.62" />
                  <polygon points="846,250 868,166 890,250" opacity="0.86" />
                  <polygon points="880,256 902,182 924,256" opacity="0.72" />
                  <polygon points="914,252 936,170 958,252" opacity="0.82" />
                  <polygon points="948,258 970,188 992,258" opacity="0.68" />
                  <polygon points="982,250 1004,160 1026,250" opacity="0.94" />
                  <polygon points="1016,256 1038,178 1060,256" opacity="0.78" />
                  <polygon points="1050,252 1072,166 1094,252" opacity="0.88" />
                  <polygon points="1084,260 1106,190 1128,260" opacity="0.7" />
                  <polygon points="1116,250 1138,158 1160,250" opacity="0.92" />
                  <polygon points="1146,256 1168,182 1190,256" opacity="0.74" />
                  <polygon points="1174,262 1196,196 1218,262" opacity="0.62" />
                </g>
                <g fill="#07111f" opacity="0.72">
                  <polygon points="-16,264 4,206 24,264" opacity="0.56" />
                  <polygon points="38,266 60,194 82,266" opacity="0.72" />
                  <polygon points="92,262 112,202 132,262" opacity="0.52" />
                  <polygon points="144,266 166,186 188,266" opacity="0.78" />
                  <polygon points="204,264 226,198 248,264" opacity="0.62" />
                  <polygon points="264,268 286,190 308,268" opacity="0.72" />
                  <polygon points="330,264 350,210 370,264" opacity="0.5" />
                  <polygon points="392,266 412,214 432,266" opacity="0.42" />
                  <polygon points="454,268 472,224 490,268" opacity="0.34" />
                  <polygon points="514,264 532,226 550,264" opacity="0.3" />
                  <polygon points="574,268 592,228 610,268" opacity="0.28" />
                  <polygon points="634,264 652,224 670,264" opacity="0.32" />
                  <polygon points="694,266 714,212 734,266" opacity="0.42" />
                  <polygon points="754,264 776,198 798,264" opacity="0.58" />
                  <polygon points="812,268 834,188 856,268" opacity="0.72" />
                  <polygon points="870,264 892,198 914,264" opacity="0.62" />
                  <polygon points="928,266 950,184 972,266" opacity="0.8" />
                  <polygon points="986,262 1008,174 1030,262" opacity="0.86" />
                  <polygon points="1044,266 1066,190 1088,266" opacity="0.72" />
                  <polygon points="1098,264 1120,180 1142,264" opacity="0.82" />
                  <polygon points="1150,268 1172,202 1194,268" opacity="0.58" />
                  <polygon points="1186,264 1208,192 1230,264" opacity="0.66" />
                  <polygon points="242,270 260,218 278,270" opacity="0.38" />
                  <polygon points="902,270 920,218 938,270" opacity="0.42" />
                </g>
              </svg>
            </div>
          </div>

          <div class="camp-scene" role="group" aria-label="Camp easter egg scene">
            <button
              class="camp-item camp-car"
              type="button"
              data-camp-item="car"
              data-camp-label="Travel van"
              aria-label="Interact with the travel van"
            >
              <span class="cast-shadow car-shadow" aria-hidden="true"></span>
              <img class="camp-visual" src="/camp/car_real.png" alt="" width="1448" height="1086" loading="eager" decoding="async" draggable="false" />
              <span class="people-reflection" aria-hidden="true"></span>
            </button>

            <button
              class="camp-item camp-people"
              type="button"
              data-camp-item="people"
              data-camp-label="Camp companions"
              aria-label="Interact with the people sitting by the campfire"
            >
              <span class="cast-shadow people-shadow" aria-hidden="true"></span>
              <img class="camp-visual" src="/camp/people.png" alt="" width="1448" height="1086" loading="eager" decoding="async" draggable="false" />
            </button>

            <button
              class="camp-item camp-fire"
              type="button"
              data-camp-item="fire"
              data-camp-label="Campfire"
              aria-label="Interact with the campfire"
            >
              <img class="camp-fire-part camp-wood" src="/camp/wood.png" alt="" width="1254" height="1254" loading="eager" decoding="async" draggable="false" />
              <img class="camp-fire-gif" src="/camp/fire.gif" alt="" loading="eager" decoding="async" draggable="false" />
            </button>

            <div class="fire-sparks" aria-hidden="true">
              <span style="--spark-drift: -7px"></span>
              <span style="--spark-drift: 5px"></span>
              <span style="--spark-drift: 12px"></span>
              <span style="--spark-drift: -2px"></span>
              <span class="spark-small" style="--spark-drift: -14px"></span>
              <span class="spark-small" style="--spark-drift: 18px"></span>
            </div>

            <button
              class="camp-item camp-dog"
              type="button"
              data-camp-item="dog"
              data-camp-label="Samoyed dog"
              aria-label="Interact with the Samoyed dog"
            >
              <span class="cast-shadow dog-shadow" aria-hidden="true"></span>
              <img class="camp-visual" src="/camp/dog.png" alt="" width="1254" height="1254" loading="eager" decoding="async" draggable="false" />
            </button>
          </div>
          <div class="fire-reflection" aria-hidden="true"></div>
          <div class="horizon" aria-hidden="true"></div>
          <div class="ground-forest forest-ground" aria-hidden="true">
            <svg viewBox="0 0 1000 240" preserveAspectRatio="none" focusable="false">
              <g fill="#050c17">
                <polygon points="-28,242 18,96 64,226" opacity="0.78" />
                <polygon points="28,230 78,48 128,242" opacity="0.94" />
                <polygon points="96,238 144,110 192,224" opacity="0.78" />
                <polygon points="156,222 210,66 264,240" />
                <polygon points="232,246 282,126 332,232" opacity="0.66" />
                <polygon points="304,228 350,136 396,244" opacity="0.52" />
                <polygon points="382,240 424,152 466,226" opacity="0.44" />
                <polygon points="464,224 504,146 544,238" opacity="0.4" />
                <polygon points="544,242 586,156 628,228" opacity="0.42" />
                <polygon points="620,230 666,128 712,244" opacity="0.52" />
                <polygon points="696,246 746,120 796,232" opacity="0.68" />
                <polygon points="764,226 812,82 860,240" opacity="0.86" />
                <polygon points="828,238 880,58 932,222" />
                <polygon points="904,224 954,106 1004,242" opacity="0.82" />
                <polygon points="960,240 1010,70 1060,228" opacity="0.92" />
              </g>
              <g fill="#091421" opacity="0.78">
                <polygon points="-14,246 24,140 62,232" opacity="0.72" />
                <polygon points="62,226 102,108 142,242" />
                <polygon points="130,242 170,146 210,230" opacity="0.68" />
                <polygon points="214,234 254,116 294,246" opacity="0.76" />
                <polygon points="286,246 322,156 358,236" opacity="0.48" />
                <polygon points="380,232 414,160 448,244" opacity="0.38" />
                <polygon points="482,246 516,176 550,234" opacity="0.34" />
                <polygon points="584,234 620,158 656,246" opacity="0.38" />
                <polygon points="668,244 704,154 740,232" opacity="0.48" />
                <polygon points="746,230 786,120 826,244" opacity="0.76" />
                <polygon points="818,242 858,108 898,226" />
                <polygon points="886,228 926,146 966,246" opacity="0.68" />
                <polygon points="958,244 998,114 1038,230" opacity="0.82" />
              </g>
            </svg>
          </div>
        </div>
      </section>

      <footer class="footer">
        <span>© {new Date().getFullYear()} {name}</span>
      </footer>
    </main>

    <div class="campfire-modal" data-campfire-modal hidden role="dialog" aria-modal="true" aria-labelledby="campfire-title">
      <div class="campfire-panel">
        <button class="campfire-close" type="button" data-campfire-close aria-label="Close campfire">×</button>
        <div class="campfire-stage">
          <div class="campfire-visual" data-campfire-drop aria-label="Drop a log into the campfire">
            <img src="/camp/fire.gif" alt="" loading="lazy" decoding="async" draggable="false" />
          </div>
          <button class="campfire-wood-stack" type="button" data-campfire-wood-stack aria-label="Drag a log into the campfire">
            <img src="/camp/wood_stack.png" alt="" loading="lazy" decoding="async" draggable="false" />
          </button>
          <img class="campfire-drag-log" data-campfire-drag-log src="/camp/wood_single.png" alt="" draggable="false" />
        </div>
        <h2 class="campfire-title" id="campfire-title">Campfire</h2>
        <p class="campfire-copy" data-campfire-copy>Checking the fire...</p>
        <div class="campfire-meter" aria-hidden="true">
          <div class="campfire-meter-label">
            <span data-campfire-wood>0 logs</span>
            <span>30 max</span>
          </div>
          <div class="campfire-track">
            <div class="campfire-fill" data-campfire-fill></div>
          </div>
        </div>
        <button type="button" data-campfire-add hidden>Add one log</button>
        <button class="campfire-secondary-action" type="button" data-campfire-extinguish>Extinguish fire</button>
        <p class="campfire-status" data-campfire-status aria-live="polite"></p>
      </div>
    </div>

    <div class="camp-dialog" data-camp-dialog hidden role="status" aria-live="polite">
      <div class="camp-dialog-panel">
        <button class="camp-dialog-close" type="button" data-camp-dialog-close aria-label="Close dialogue">×</button>
        <p class="camp-dialog-speaker" data-camp-dialog-speaker></p>
        <p class="camp-dialog-line" data-camp-dialog-line></p>
      </div>
    </div>

    <div class="lighthouse-entry" data-lighthouse-entry hidden aria-live="polite">
      <div class="lighthouse-entry-panel">
        <h2 class="lighthouse-entry-title">Lighthouse</h2>
        <p class="lighthouse-entry-copy">What's in this lighthouse?</p>
        <div class="lighthouse-entry-actions">
          <a class="lighthouse-entry-link" href="/lighthouse" data-lighthouse-enter>Enter Lighthouse</a>
          <button class="lighthouse-entry-cancel" type="button" data-lighthouse-cancel>Stay Outside</button>
        </div>
      </div>
    </div>

    <script>
      if ("scrollRestoration" in history) {
        history.scrollRestoration = "manual";
      }
      window.scrollTo(0, 0);

      const planets = Array.from(document.querySelectorAll(".planet"));
      const title = document.querySelector("[data-info-title]");
      const orbit = document.querySelector("[data-info-orbit]");
      const description = document.querySelector("[data-info-description]");
      const card = document.querySelector(".info-card");
      const dome = document.querySelector(".dome");

      const activatePlanet = (planet) => {
        planets.forEach((item) => item.classList.toggle("is-active", item === planet));
        if (title) title.textContent = planet.dataset.title || "";
        if (orbit) orbit.textContent = planet.dataset.orbit || "";
        if (description) description.textContent = planet.dataset.description || "";
        if (card) {
          const dome = planet.closest(".dome");
          const domeRect = dome.getBoundingClientRect();
          const planetRect = planet.getBoundingClientRect();
          const cardWidth = Math.min(292, domeRect.width - 36);
          const cardHeight = card.offsetHeight;
          const planetCenter = planetRect.left - domeRect.left + planetRect.width / 2;
          let left = planetCenter - cardWidth / 2;
          let top = planetRect.top - domeRect.top - cardHeight - 22;

          left = Math.min(Math.max(18, left), domeRect.width - cardWidth - 18);

          if (top < 18) {
            top = planetRect.bottom - domeRect.top + 22;
          }

          card.style.left = `${Math.max(18, left)}px`;
          card.style.top = `${Math.max(18, top)}px`;
          card.classList.add("is-visible");
          card.setAttribute("aria-hidden", "false");
        }
      };

      const hideCard = () => {
        planets.forEach((item) => item.classList.remove("is-active"));
        if (card) {
          card.classList.remove("is-visible");
          card.setAttribute("aria-hidden", "true");
        }
      };

      planets.forEach((planet) => {
        planet.addEventListener("mouseenter", () => activatePlanet(planet));
        planet.addEventListener("mouseleave", hideCard);
        planet.addEventListener("focus", () => activatePlanet(planet));
        planet.addEventListener("blur", hideCard);
      });


      const campItems = Array.from(document.querySelectorAll("[data-camp-item]"));

      const emitCampEvent = (name, item, extra = {}) => {
        const detail = {
          id: item.dataset.campItem || "",
          label: item.dataset.campLabel || "",
          element: item,
          ...extra,
        };
        window.dispatchEvent(new CustomEvent(`camp:${name}`, { detail }));
      };

      campItems.forEach((item) => {
        item.addEventListener("mouseenter", () => {
          item.classList.add("is-camp-hovered");
          emitCampEvent("hover", item);
        });

        item.addEventListener("mouseleave", () => {
          item.classList.remove("is-camp-hovered");
          emitCampEvent("leave", item);
        });

        item.addEventListener("focus", () => {
          item.classList.add("is-camp-hovered");
          emitCampEvent("focus", item);
        });

        item.addEventListener("blur", () => {
          item.classList.remove("is-camp-hovered", "is-camp-active");
          emitCampEvent("blur", item);
        });

        item.addEventListener("click", () => {
          campItems.forEach((other) => {
            if (other !== item) other.classList.remove("is-camp-active");
          });
          item.classList.toggle("is-camp-active");
          emitCampEvent("click", item, {
            active: item.classList.contains("is-camp-active"),
          });
        });

        item.addEventListener("keydown", (event) => {
          if (item.tagName.toLowerCase() === "button") return;
          if (event.key !== "Enter" && event.key !== " ") return;
          event.preventDefault();
          item.dispatchEvent(new MouseEvent("click", { bubbles: true }));
        });
      });

      const DAY_MS = 24 * 60 * 60 * 1000;
      const MAX_WOOD = 30;
      const LOCAL_CAMPFIRE_KEY = "operation-astro-campfire";
      const CAMP_WOOD_SRC = "/camp/wood.png";
      const CAMP_FIRE_SRC = "/camp/fire.gif";
      const CAMP_SMOKE_SRC = "/camp/smoke.gif";
      const CAMP_WOOD_SINGLE_SRC = "/camp/wood_single.png";
      const CAMP_DOG_SRC = "/camp/dog.png";
      const CAMP_DOG_SLEEP_SRC = "/camp/dog_sleep.png";
      const EXTINGUISHED_DIALOGUE = {
        people: {
          speaker: "WW:",
          line: "It's freezing out here. We should add some wood to keep the fire going.",
        },
        dog: {
          speaker: "YY:",
          line: "zzz...zzz...",
        },
      };
      const campfireModal = document.querySelector("[data-campfire-modal]");
      const campfireClose = document.querySelector("[data-campfire-close]");
      const campfireAdd = document.querySelector("[data-campfire-add]");
      const campfireExtinguish = document.querySelector("[data-campfire-extinguish]");
      const campfireCopy = document.querySelector("[data-campfire-copy]");
      const campfireWood = document.querySelector("[data-campfire-wood]");
      const campfireFill = document.querySelector("[data-campfire-fill]");
      const campfireStatus = document.querySelector("[data-campfire-status]");
      const campfireDrop = document.querySelector("[data-campfire-drop]");
      const campfireWoodStack = document.querySelector("[data-campfire-wood-stack]");
      const campfireDragLog = document.querySelector("[data-campfire-drag-log]");
      const campWoodImage = document.querySelector(".camp-wood");
      const campfireVisualImage = document.querySelector(".campfire-visual img");
      const campDogImage = document.querySelector(".camp-dog .camp-visual");
      const campDialog = document.querySelector("[data-camp-dialog]");
      const campDialogClose = document.querySelector("[data-camp-dialog-close]");
      const campDialogSpeaker = document.querySelector("[data-camp-dialog-speaker]");
      const campDialogLine = document.querySelector("[data-camp-dialog-line]");
      const lighthouseEntry = document.querySelector("[data-lighthouse-entry]");
      const lighthouseEnter = document.querySelector("[data-lighthouse-enter]");
      const lighthouseCancel = document.querySelector("[data-lighthouse-cancel]");
      let campfireSource = "api";
      let isCampfireExtinguished = false;
      let campfireState = { burnUntil: 0 };
      let isAddingCampfireWood = false;
      let isDraggingCampfireWood = false;
      let lighthouseTimer = 0;

      const getCampfireSnapshot = (state = {}) => {
        const now = Date.now();
        const burnUntil = Number.isFinite(state.burnUntil) ? state.burnUntil : 0;
        const remainingMs = Math.max(0, burnUntil - now);
        const woodCount = Math.min(MAX_WOOD, Math.ceil(remainingMs / DAY_MS));
        return {
          burnUntil,
          remainingMs,
          woodCount,
          isFull: woodCount >= MAX_WOOD,
        };
      };

      const formatRemaining = (remainingMs) => {
        const totalMinutes = Math.max(0, Math.ceil(remainingMs / (60 * 1000)));
        const days = Math.floor(totalMinutes / (24 * 60));
        const hours = Math.floor((totalMinutes - days * 24 * 60) / 60);
        const minutes = totalMinutes - days * 24 * 60 - hours * 60;
        return `${days} d ${hours} h ${minutes} m`;
      };

      const readLocalCampfire = () => {
        try {
          return getCampfireSnapshot(JSON.parse(localStorage.getItem(LOCAL_CAMPFIRE_KEY) || "{}"));
        } catch {
          return getCampfireSnapshot();
        }
      };

      const writeLocalCampfire = (state) => {
        localStorage.setItem(LOCAL_CAMPFIRE_KEY, JSON.stringify({ burnUntil: state.burnUntil }));
      };

      const closeCampDialogue = () => {
        if (campDialog) campDialog.hidden = true;
      };

      const showCampDialogue = (dialogue) => {
        if (!campDialog || !dialogue) return;
        if (campDialogSpeaker) campDialogSpeaker.textContent = dialogue.speaker;
        if (campDialogLine) campDialogLine.textContent = dialogue.line;
        campDialog.hidden = false;
      };

      const applyCampfireScene = (snapshot) => {
        const isExtinguished = snapshot.woodCount === 0 && snapshot.remainingMs <= 0;
        isCampfireExtinguished = isExtinguished;
        dome?.classList.toggle("is-camp-extinguished", isExtinguished);
        campfireModal?.classList.toggle("is-extinguished", isExtinguished);
        if (campWoodImage) campWoodImage.src = isExtinguished ? CAMP_SMOKE_SRC : CAMP_WOOD_SRC;
        if (campfireVisualImage) campfireVisualImage.src = isExtinguished ? CAMP_SMOKE_SRC : CAMP_FIRE_SRC;
        if (campDogImage) campDogImage.src = isExtinguished ? CAMP_DOG_SLEEP_SRC : CAMP_DOG_SRC;
        if (!isExtinguished) closeCampDialogue();
      };

      const renderCampfire = (state, status = "") => {
        campfireState = { burnUntil: Number.isFinite(state?.burnUntil) ? state.burnUntil : 0 };
        const snapshot = getCampfireSnapshot(state);
        applyCampfireScene(snapshot);
        if (campfireCopy) campfireCopy.textContent = formatRemaining(snapshot.remainingMs);
        if (campfireWood) campfireWood.textContent = `${snapshot.woodCount} log${snapshot.woodCount === 1 ? "" : "s"}`;
        if (campfireFill) campfireFill.style.setProperty("--campfire-level", `${Math.round((snapshot.woodCount / MAX_WOOD) * 100)}`);
        if (campfireAdd) {
          campfireAdd.disabled = snapshot.isFull || isAddingCampfireWood;
          campfireAdd.textContent = snapshot.isFull ? "The fire is full" : "Add one log";
        }
        if (campfireWoodStack) {
          const isDisabled = snapshot.isFull || isAddingCampfireWood;
          campfireWoodStack.disabled = isDisabled;
          campfireWoodStack.setAttribute("aria-disabled", String(isDisabled));
          campfireWoodStack.setAttribute(
            "aria-label",
            snapshot.isFull ? "The campfire already has thirty logs" : "Drag a log into the campfire",
          );
        }
        if (campfireExtinguish) {
          campfireExtinguish.disabled = snapshot.woodCount === 0 && snapshot.remainingMs <= 0;
        }
        if (campfireStatus) campfireStatus.textContent = status;
      };

      const fetchCampfire = async () => {
        try {
          const response = await fetch("/api/campfire", {
            headers: { Accept: "application/json" },
            cache: "no-store",
          });
          if (!response.ok) throw new Error("Campfire API unavailable");
          campfireSource = "api";
          const state = await response.json();
          renderCampfire(state);
        } catch {
          campfireSource = "local";
          renderCampfire(readLocalCampfire(), "Local preview. Cloudflare KV will keep the shared fire online.");
        }
      };

      const refreshCampfireClock = () => {
        if (!campfireModal || campfireModal.hidden) return;
        renderCampfire(campfireState, campfireStatus?.textContent || "");
      };

      const addCampfireWood = async () => {
        const currentSnapshot = getCampfireSnapshot(campfireState);
        if (isAddingCampfireWood || currentSnapshot.isFull) return;
        isAddingCampfireWood = true;
        if (campfireAdd) campfireAdd.disabled = true;
        if (campfireWoodStack) campfireWoodStack.disabled = true;
        if (campfireStatus) campfireStatus.textContent = "Adding a log...";

        if (campfireSource === "local") {
          const current = readLocalCampfire();
          const base = Math.max(Date.now(), current.burnUntil);
          const next = getCampfireSnapshot({ burnUntil: Math.min(Date.now() + MAX_WOOD * DAY_MS, base + DAY_MS) });
          writeLocalCampfire(next);
          isAddingCampfireWood = false;
          renderCampfire(next, "Saved in this browser for local preview.");
          return;
        }

        try {
          const response = await fetch("/api/campfire/add-wood", {
            method: "POST",
            headers: { Accept: "application/json" },
          });
          const state = await response.json();
          if (!response.ok) throw new Error(state.error || "Could not add wood");
          isAddingCampfireWood = false;
          renderCampfire(state, "One log added.");
        } catch {
          campfireSource = "local";
          const current = readLocalCampfire();
          const base = Math.max(Date.now(), current.burnUntil);
          const next = getCampfireSnapshot({ burnUntil: Math.min(Date.now() + MAX_WOOD * DAY_MS, base + DAY_MS) });
          writeLocalCampfire(next);
          isAddingCampfireWood = false;
          renderCampfire(next, "Using local preview until the Cloudflare KV binding is ready.");
        }
      };

      const moveCampfireDragLog = (clientX, clientY) => {
        if (!campfireDragLog) return;
        campfireDragLog.style.transform = `translate3d(${clientX - 58}px, ${clientY - 34}px, 0) rotate(-8deg)`;
      };

      const isPointInsideCampfireDrop = (clientX, clientY) => {
        if (!campfireDrop) return false;
        const rect = campfireDrop.getBoundingClientRect();
        return clientX >= rect.left && clientX <= rect.right && clientY >= rect.top && clientY <= rect.bottom;
      };

      const endCampfireWoodDrag = (event) => {
        if (!isDraggingCampfireWood) return;
        isDraggingCampfireWood = false;
        campfireDragLog?.classList.remove("is-visible");
        if (campfireWoodStack?.hasPointerCapture?.(event.pointerId)) {
          campfireWoodStack.releasePointerCapture(event.pointerId);
        }
        if (isPointInsideCampfireDrop(event.clientX, event.clientY)) {
          addCampfireWood();
        }
      };

      const startCampfireWoodDrag = (event) => {
        const snapshot = getCampfireSnapshot(campfireState);
        if (snapshot.isFull || isAddingCampfireWood || !campfireDragLog) return;
        event.preventDefault();
        isDraggingCampfireWood = true;
        campfireDragLog.src = CAMP_WOOD_SINGLE_SRC;
        campfireWoodStack?.setPointerCapture?.(event.pointerId);
        moveCampfireDragLog(event.clientX, event.clientY);
        campfireDragLog.classList.add("is-visible");
      };

      const extinguishCampfire = async () => {
        if (!campfireExtinguish) return;
        campfireExtinguish.disabled = true;
        if (campfireStatus) campfireStatus.textContent = "Letting the fire go dark...";

        const next = getCampfireSnapshot({ burnUntil: 0, updatedAt: Date.now() });

        if (campfireSource === "local") {
          writeLocalCampfire(next);
          renderCampfire(next, "The campfire is out in local preview.");
          return;
        }

        try {
          const response = await fetch("/api/campfire/extinguish", {
            method: "POST",
            headers: { Accept: "application/json" },
          });
          const state = await response.json();
          if (!response.ok) throw new Error(state.error || "Could not extinguish fire");
          renderCampfire(state, "The campfire is out.");
        } catch {
          campfireSource = "local";
          writeLocalCampfire(next);
          renderCampfire(next, "Using local preview until the Cloudflare KV binding is ready.");
        }
      };

      const openCampfire = () => {
        if (!campfireModal) return;
        campfireModal.hidden = false;
        fetchCampfire();
        campfireWoodStack?.focus();
      };

      const closeCampfire = () => {
        if (campfireModal) campfireModal.hidden = true;
      };

      const closeLighthouseEntry = () => {
        window.clearTimeout(lighthouseTimer);
        lighthouseTimer = 0;
        dome?.classList.remove("is-lighthouse-focus");
        document.body.classList.remove("is-lighthouse-focus");
        if (lighthouseEntry) {
          lighthouseEntry.classList.remove("is-visible");
          lighthouseEntry.hidden = true;
        }
        requestCampPerspective();
      };

      const openLighthouseEntry = () => {
        if (!dome || !lighthouseEntry) return;
        closeCampfire();
        closeCampDialogue();
        hideCard();
        campItems.forEach((item) => item.classList.remove("is-camp-active", "is-camp-hovered"));
        lighthouseEntry.hidden = false;
        lighthouseEntry.classList.remove("is-visible");
        dome.classList.add("is-lighthouse-focus");
        document.body.classList.add("is-lighthouse-focus");
        window.clearTimeout(lighthouseTimer);
        window.scrollTo({ top: 0, behavior: "smooth" });
        lighthouseTimer = window.setTimeout(() => {
          lighthouseEntry.classList.add("is-visible");
          lighthouseEnter?.focus();
        }, 1850);
      };

      window.addEventListener("camp:click", (event) => {
        const id = event.detail?.id;
        if (id === "lighthouse") {
          openLighthouseEntry();
          return;
        }

        if (dome?.classList.contains("is-lighthouse-focus")) {
          return;
        }

        if (id === "fire") {
          closeCampDialogue();
          openCampfire();
          return;
        }

        if (isCampfireExtinguished && (id === "people" || id === "dog")) {
          showCampDialogue(EXTINGUISHED_DIALOGUE[id]);
          return;
        }

        closeCampDialogue();
      });

      campfireAdd?.addEventListener("click", addCampfireWood);
      campfireWoodStack?.addEventListener("pointerdown", startCampfireWoodDrag);
      campfireWoodStack?.addEventListener("pointermove", (event) => {
        if (!isDraggingCampfireWood) return;
        moveCampfireDragLog(event.clientX, event.clientY);
      });
      campfireWoodStack?.addEventListener("pointerup", endCampfireWoodDrag);
      campfireWoodStack?.addEventListener("pointercancel", endCampfireWoodDrag);
      campfireExtinguish?.addEventListener("click", extinguishCampfire);
      campfireClose?.addEventListener("click", closeCampfire);
      campDialogClose?.addEventListener("click", closeCampDialogue);
      lighthouseCancel?.addEventListener("click", closeLighthouseEntry);
      campfireModal?.addEventListener("click", (event) => {
        if (event.target === campfireModal) closeCampfire();
      });
      window.addEventListener("keydown", (event) => {
        if (event.key === "Escape") {
          closeCampfire();
          closeCampDialogue();
          closeLighthouseEntry();
        }
      });

      fetchCampfire();
      window.setInterval(refreshCampfireClock, 60 * 1000);

      const updateCampPerspective = () => {
        if (!dome) return;
        const maxLift = Math.min(720, Math.max(420, window.innerHeight * 0.6));
        const travel = Math.max(1, window.innerHeight * 1.35);
        const progress = Math.min(1, Math.max(0, window.scrollY / travel));
        const eased = 1 - Math.pow(1 - progress, 2);
        const campLift = Math.round(eased * maxLift);
        dome.style.setProperty("--camp-lift", `${campLift}px`);
        dome.style.setProperty("--ridge-far-lift", `${Math.round(campLift * 1)}px`);
        dome.style.setProperty("--ridge-middle-lift", `${Math.round(campLift * 1.2)}px`);
        dome.style.setProperty("--forest-far-lift", `${Math.round(campLift * 1.4)}px`);
        dome.style.setProperty("--planet-depth", `${(1 - eased * 0.4).toFixed(3)}`);
      };

      let scrollFrame = 0;
      const requestCampPerspective = () => {
        if (scrollFrame) return;
        scrollFrame = window.requestAnimationFrame(() => {
          scrollFrame = 0;
          updateCampPerspective();
        });
      };

      window.addEventListener("scroll", requestCampPerspective, { passive: true });
      window.addEventListener("resize", requestCampPerspective);
      updateCampPerspective();
    </script>
  </body>
</html>
